# Visual prompt record

Project: Brave Portable Updater

Product statement: A path-scoped Windows PowerShell updater for an existing Portapps Brave bundle. It updates the portable browser, checks the publisher, supports profile backup and rollback, and leaves the system-installed browser alone by default.

## Shared output requirements

Logo files must be true RGBA PNGs. Pixels outside the mark must have alpha set to zero. Do not draw a checkerboard, solid canvas, watermark, Brave lion, browser trademark, or unrelated product interface.

## Container and recovery route

Create a premium symbol that combines a protected portable container, a precise update path, and recoverability. Use deep midnight navy, electric cyan, and one restrained verification-green accent. Keep a strong silhouette and generous padding. Avoid cartoon styling, excess detail, glow, and obvious stock-icon construction.

## Scoped application window

Reduce the idea to an outer rounded boundary, an inner application tile, and one directional path. The outer boundary represents the system installation that remains unchanged. The inner tile is the portable bundle. Use no more than three bold shapes and keep the mark readable at 24 pixels.

## BP monogram

Build an interlocking BP monogram from one precise route that turns inward. Use Swiss geometric discipline, balanced counters, and a compact square silhouette. Use solid midnight navy for the main form and electric cyan for one short path segment. Do not add an app-icon plate.

## Shield and recovery emblem

Create a restrained open shield whose lower edge becomes a returning recovery path. Hold one portable application tile inside. The outer shield stays visually still while the inner path moves. Use a mature security and infrastructure tone without locks, antivirus styling, circular sync arrows, or download arrows.

## README hero

Create a polished 2:1 GitHub README hero. Put the selected shield-and-recovery mark and benefit-led copy on the left. Place the supplied real WhatIf console capture on the right in a restrained Windows terminal frame. Use a midnight navy background, a subtle cyan edge light, disciplined spacing, and clear hierarchy.

Exact copy:

```text
POWERSHELL FOR WINDOWS
Brave Portable Updater
Update the portable browser.
Leave the installed one alone.
Path-scoped updates with publisher checks, profile backup, and rollback.
PowerShell 5.1+
Windows 10/11
```

Preserve the supplied terminal content. Do not invent commands, versions, paths, statuses, or extra interface elements.
